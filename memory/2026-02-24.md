# Durable Memory - 2026-02-24

## Projects

### dot-sfty-core-analytics (Falcon Classification)
- **Status**: Training parallelized across all sizes (n, s, m, l, x) on Google Batch.
- **Key Files Created**:
  - `google_batch/scripts/falcon_classification/job.json` (Template for all sizes)
  - `google_batch/scripts/falcon_classification/script.sh` (Build/Push/Submit wrapper)
  - `google_batch/scripts/falcon_classification/submit_all_sizes.sh` (Parallel job launcher)
  - `google_batch/falcon_classification_train.py` (Updated to accept `--model_size` and optimized with `imgsz=224`)
- **Fixes**:
  - Used `python3.11` to resolve `ModuleNotFoundError` for `ultralytics`.
  - Aligned `allocationPolicy` GPU count with `device` parameters to fix `ValueError`.
  - Cleaned up `dataset.tar.gz` (excluded `.cache` and `.DS_Store`) before GCS upload to fix pathing/loading errors.
- **Insights**:
  - `docker prune` causes slow first builds due to the massive size of `tensorrt-cu12` and the `nvidia/cuda:devel` base image.
  - Optimization to `imgsz=224` and `batch=32` derived from successful local benchmarks.

### James Note Strategy
- **Prompt Optimization**: Updated the article generation prompt to require "Rationale," "Shared Pain (HARM)," and "Failure Stories" to increase reader empathy and engagement.
- **Rule**: Avoid over-mentioning "CleanShot X" to prevent an "AI-generated" vibe. Focus on broader concepts like "Visual First" and "Async Work."
- **Content Pillars**: Balanced rotation maintained across Career Survival, Backdoor Routes, English Hacks, and Product ROI.

### Ghost Root (Hacker Game)
- **Cycle 255 (The Process Trace)**: Advanced to Phase 12.0.
- **Features**: Consolidated `strace` simulation in `Shell.ts`, added dynamic PIDs, `ltrace` support, and argument tracing (`execve`).
- **Solution**: Tracing `mystery_process` reveals a missing config at `/tmp/secret_config.dat`.

## Dataset Progress
- **Classification Dataset**:
  - `hand` class samples reduced (1/10 then further 1/5) to balance dataset.
  - `others` class split into `train:valid:test` at 8:1:1 ratio.
  - Uploaded to `gs://yolo-gcp/falcon/train/input/classification/v1/dataset.tar.gz`.
