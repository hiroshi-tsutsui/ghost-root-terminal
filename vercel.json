{
  "framework": "nextjs",
  "buildCommand": "cd project_omega && npm install && npm run build",
  "outputDirectory": "project_omega/.next",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./project_omega/"
}